{
  "type": "module",
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^7.4.2",
    "fastify": "^5.7.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.18.0"
  },
  "scripts": {
    "dev": "prisma generate && prisma migrate deploy && tsx watch server.ts",
    "db:migrate": "npx prisma migrate dev",
    "db:studio": "npx prisma studio --browser none --hostname 0.0.0.0",
    "db:gen": "npx prisma generate"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "dotenv": "^17.3.1",
    "prisma": "^6.19.2",
    "tsx": "^4.21.0"
  }
}